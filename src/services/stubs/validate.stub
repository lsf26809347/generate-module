<?php

namespace {%namespace%};

use think\Validate;

class {%className%} extends {%extends%}
{
    /**
     * 定义验证规则
     * 格式：'字段名'	=>	['规则1','规则2'...]
     *
     * @var array
     */
    protected $rule = [

    ];

    /**
     * 定义错误信息
     * 格式：'字段名.规则名'	=>	'错误信息'
     *
     * @var array
     */
    protected $field = [

    ];

    /**
     * 编辑场景，可以去除编辑场景不验证的字段
     */
    public function sceneEdit()
    {
//      return $this->remove('title','unique');
    }
}
